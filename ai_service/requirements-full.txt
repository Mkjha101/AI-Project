# Full ML requirements (may require conda or a specific Python version like 3.11)
# Install these only if you need full model functionality and have a compatible setup.

# PyTorch (install via the official index or conda; see https://pytorch.org)
# torch==2.0.1
# torchvision==0.15.2

# Ultralytics (YOLO) - depends on torch
# ultralytics==8.0.181

# TensorFlow (may require specific wheel or conda)
# tensorflow==2.13.0

# Transformers / NLP
# transformers==4.33.2

# OpenCV - if you need CV bindings; prefer headless on servers
# opencv-python==4.8.1.78

# Notes:
# - For Windows it's strongly recommended to use conda/miniforge for installing
#   PyTorch and TensorFlow. Example (conda):
#     conda create -n ai python=3.11
#     conda activate ai
#     conda install pytorch torchvision cpuonly -c pytorch
#     pip install -r requirements.txt
# - Alternatively, install PyTorch from the official wheel index:
#     python -m pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
