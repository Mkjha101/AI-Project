# Minimal development requirements (exclude heavy ML packages like torch/tensorflow/ultralytics)
# Use this for quick local testing of the Flask endpoints and fallback logic.
# CRITICAL: Pin versions tightly to avoid numpy/scikit-learn/pandas incompatibility

# Core web framework
flask==2.3.3
requests>=2.31
flask-cors>=3.0.10
python-dotenv>=1.0.0

# Numerical & data processing (numpy 1.26 for compatibility with scikit-learn 1.3.2)
numpy==1.26.4
pandas==2.3.3
Pillow>=9.5

# Image processing (compatible with numpy 1.26)
opencv-python-headless==4.8.1.78

# ML & statistical (scikit-learn 1.3.2 WORKS with numpy 1.26; newer versions require numpy 2.x)
scikit-learn==1.3.2

# Optional: only for ASGI apps (not needed for Flask)
# uvicorn==0.23.2
